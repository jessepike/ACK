# Coding Style

## TypeScript / JavaScript
- Use `const` over `let`. Avoid `var`.
- Use async/await over raw promises.
- Strict type checking.

## CSS
- Use utility classes (if Tailwind) or localized CSS modules.
- Avoid global style pollution.
